<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.puropoo.proyectobys">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- SplashScreen -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Menú principal -->
        <activity
            android:name=".MenuActivity"
            android:exported="true" >
        </activity>

        <!-- Registro de clientes -->
        <activity
            android:name=".MainActivity"
            android:exported="true" >
        </activity>

        <!-- Listado de clientes -->
        <activity
            android:name=".ClientsListActivity"
            android:exported="true" >
        </activity>
        <activity android:name=".RegisterTeamActivity"/>
        <activity android:name=".RegisterRequirementsActivity"/>
        <activity android:name=".RegisterEquipmentActivity"/>
        <activity android:name=".RegisterSecondVisitActivity"/>
        <activity android:name=".RegisterRequestActivity" />
        <activity android:name=".ViewRequestsActivity"></activity>
        <activity android:name=".EditRequestActivity"></activity>
        <activity android:name=".RegisterMembersActivity">
            <!-- Puedes agregar algún filtro de intent si es necesario -->
        </activity>
        <activity android:name=".RegisterMaintenanceRequirementsActivity">
            <!-- Puedes agregar otros filtros de intent si es necesario -->
        </activity>
    </application>
</manifest>
